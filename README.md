# demos-os-android

Demos for Android, including NDK internals and Java.

`apps` holds the sample applications, `c` the native code, `exercises`
the course exercises, and `android_build` the shared Gradle build setup.
